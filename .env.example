# Porter Environment Configuration
# Copy this file to .env and fill in the values

# GitHub Token
# Required for: Cloning private repos, creating PR comments, Gist config storage
# Get it from: https://github.com/settings/tokens
# Scopes needed: repo (full access), gist
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Desktop Daemon Configuration
PORTER_ADDR=:3000

# Agent Paths (Optional)
# If your agent binaries are not in a standard location, set them here
# AIDER_PATH=/path/to/aider
# OPENCODE_PATH=/path/to/opencode

# Web App Configuration
# GitHub OAuth credentials
# Get from GitHub App settings -> OAuth App section
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_OAUTH_REDIRECT_URI=http://localhost:5173/api/auth/github/callback

# Session signing secret
# Generate with: openssl rand -hex 32
SESSION_SECRET=

# Webhook verification secret
# Set in GitHub App Webhooks -> Secret
WEBHOOK_SECRET=

# GitHub App credentials (required for installation token minting)
GITHUB_APP_ID=
GITHUB_APP_PRIVATE_KEY=

# GitHub App install link (used in onboarding)
# Example: https://github.com/apps/<your-app-name>/installations/new
PUBLIC_GITHUB_APP_INSTALL_URL=

# Optional: comma-separated GitHub logins allowed to run webhook-triggered
# production machine launches during rollout.
PORTER_PRODUCTION_ALLOWLIST=cloudboy-jh

# Optional: file path for persisting pending execution contexts.
# Defaults to a temp file when unset.
PORTER_EXECUTION_STORE_PATH=

# Optional: encryption secret and storage path for persisted OAuth tokens
# used to read user private gists from webhook context.
PORTER_OAUTH_TOKEN_SECRET=
PORTER_OAUTH_TOKEN_STORE_PATH=
